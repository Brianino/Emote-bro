var commands = {};

commands.help = function () {
	//Help commandsmand
};
commands.count = function () {
	//Get Server Count
}
commands.id = function () {
	//Server search by id
};
commands.name = function () {
	//Server search by name
};
commands.emote = function () {
	//Emote Search
};
commands.refresh = function () {
	//Reload Server List
};
commands.log = function () {
	//Log global variables
};
commands.disp = function () {
	//Display server at specified index
};
commands.add = function () {
	//Grant user locked bot commands
};
commands.remove = function () {
	//Lock commands from user 
};
commands.blacklist = function () {
	//Ignore commands in specified channel
	//or remove from blacklist
};
commands.prefix = function () {
	//Change bot commands prefix
};
commands.quit = function () {
	//Shutdown bot
};

module.exports = commands;